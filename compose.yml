services:
  # node:
  #   image: care-capsule:latest
  #   environment:
  #     PORT: ${PORT:-3000}
  #     NODE_ENV: ${NODE_ENV}
  #     DB_HOST: db
  #     DB_USER: ${DB_USER}
  #     DB_PASS: ${DB_PASS}
  #     DB_NAME: ${DB_NAME}
  #     DB_PORT: ${DB_PORT}
  #     JWT_ACCESS: ${JWT_ACCESS}
  #     JWT_REFRESH: ${JWT_REFRESH}
  #     JWT_ADMIN: ${JWT_ADMIN}
  #   ports:
  #     - '${PORT}:${PORT}'
  #   networks:
  #     - backend
  #   depends_on:
  #     - db
  #   healthcheck:
  #     test: ['CMD-SHELL', 'curl -f http://localhost:${PORT}/health || exit 1']
  #     interval: 30s
  #     timeout: 15s
  #     retries: 3
  #     start_period: 90s
  #     start_interval: 10s

  db:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: carecapsule
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - backend

volumes:
  postgres-data:

networks:
  backend:
    driver: bridge
